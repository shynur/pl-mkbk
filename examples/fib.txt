LET arr = [0, 1];
LET fib;
fib = |n| {
    IF n < Len(arr) {
        arr[n]
    } ELSE IF n == Len(arr) {
        LET last = arr[n-1] + arr[n-2];
        arr.append(last);
        last
    } ELSE {
        fib(n-1) + fib(n-2)
    }
};
# 斐波那契数列
